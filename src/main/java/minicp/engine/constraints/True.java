package minicp.engine.constraints;

import minicp.engine.core.AbstractConstraint;
import minicp.engine.core.Solver;

public class True extends AbstractConstraint {
    public True(Solver cp) {
        super(cp);
    }
}
